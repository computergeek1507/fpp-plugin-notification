#include "pushover.h"

#include <cstdint>
#include <thread>
#include <cmath>

pushover::pushover(std::string const& key) :
    notificationBase(ip,port,item,startChannel)
}

pushover::~pushover() {

}
